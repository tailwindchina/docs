---
title: "Grid Auto Rows"
# description: "Utilities for controlling the size of implicitly-created grid rows."
description: "用于控制隐式创建的网格行的大小的功能类。"
---

import plugin from 'tailwindcss/lib/plugins/gridAutoRows'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'

export const classes = { plugin }

<!-- ## Usage -->
## 使用方法

<!-- Use the `auto-rows-{size}` utilities to control the size implicitly-created grid rows. -->
使用 `auto-rows-{size}` 功能类来控制隐式创建的网格行的大小。

```html
<div class="grid grid-flow-row **auto-rows-max**">
  <div>1</div>
  <div>2</div>
  <div>3</div>
</div>
```

<!-- ## Responsive -->
## 响应式

<!-- To control the grid-auto-rows property at a specific breakpoint, add a `{screen}:` prefix to any existing grid-auto-rows utility. For example, use `md:auto-rows-min` to apply the `auto-rows-min` utility at only medium screen sizes and above. -->
要在特定的断点处控制 grid-auto-rows 属性，可以在任何现有的 grid-auto-rows 功能类前添加 `{screen}:` 前缀。例如，使用 `md:auto-rows-min` 来仅在中等尺寸及以上的屏幕应用 `auto-rows-min` 功能。

```html
<div class="grid grid-flow-row auto-rows-max **md:auto-rows-min**">
  <div>1</div>
  <div>2</div>
  <div>3</div>
</div>
```

<!-- For more information about Tailwind's responsive design features, check out the [Responsive Design](/docs/responsive-design) documentation. -->
关于 Tailwind 的响应式设计功能的更多信息，请查看[响应式设计](/docs/responsive-design)文档。

<!-- ## Customizing -->
## 定制

<!-- By default Tailwind includes four general purpose grid-auto-rows utilities. You can customize these in the `theme.gridAutoRows` section of your `tailwind.config.js` file. -->
默认情况下，Tailwind 包括四个通用的 grid-auto-rows 功能类。您可以在您的 `tailwind.config.js` 文件的 `theme.gridAutoRows` 部分定制这些功能类。

```js
  // tailwind.config.js
  module.exports = {
    theme: {
      extend: {
        gridAutoRows: {
          '2fr': 'minmax(0, 2fr)',
        }
      }
    }
  }
```

<!-- Learn more about customizing the default theme in the [theme customization documentation](/docs/theme#customizing-the-default-theme). -->
在 [主题自定义文档](/docs/theme#customizing-the-default-theme) 中了解更多关于自定义默认主题的信息。

<!-- ### Variants -->
### 变体

<Variants plugin="gridAutoRows" name="grid-auto-rows" />

<!-- ### Disabling -->
### 禁止

<Disabling plugin="gridAutoRows" name="grid-auto-rows" />
