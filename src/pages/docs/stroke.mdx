---
title: "Stroke"
# description: "Utilities for styling the stroke of SVG elements."
description: "用于风格化 SVG 元素的 stroke 功能类。"
---

import plugin from 'tailwindcss/lib/plugins/stroke'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'

export const classes = { plugin }

<!-- ## Usage -->
## 使用

<!-- Use `stroke-current` to set the stroke color of an SVG to the current text color. This makes it easy to set an element's stroke color by combining this class with an existing [text color utility](/docs/text-color). -->

<!-- Useful for styling icon sets like [Heroicons](http://heroicons.com/) that are drawn entirely with strokes. -->
使用 `stroke-current` 将 SVG 的 `stroke` 颜色设置为当前的文本颜色。通过将该类与现有的[文本颜色功能类](/docs/text-color)相结合，可以轻松设置元素的 `stroke` 颜色。

对于像 [Heroicons](http://heroicons.com/) 这样完全用 `stroke` 绘制的图标集很有用。

```html purple
<template preview>
  <div class="text-center">
    <svg class="stroke-current text-purple-600 inline-block h-12 w-12" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <circle cx="8" cy="21" r="2" />
      <circle cx="20" cy="21" r="2" />
      <path d="M5.67 6H23l-1.68 8.39a2 2 0 0 1-2 1.61H8.75a2 2 0 0 1-2-1.74L5.23 2.74A2 2 0 0 0 3.25 1H1" />
    </svg>
  </div>
</template>

<svg class="**stroke-current text-purple-600** ..."></svg>
```

<!-- ## Customizing -->
## 自定义

<!-- Control which stroke utilities Tailwind generates by customizing the `theme.stroke` section in your `tailwind.config.js` file: -->
通过在你的 `tailwind.config.js` 文件中自定义 `theme.stroke` 部分来控制 Tailwind 生成哪些 `stroke` 功能类。

```diff-js
  // tailwind.config.js
  module.exports = {
    theme: {
-     stroke: {
-       current: 'currentColor',
-     }
+     stroke: theme => ({
+       'red': theme('colors.red.500'),
+       'green': theme('colors.green.500'),
+       'blue': theme('colors.blue.500'),
+     })
    }
  }
```

<!-- ### Variants -->
### 变体

<Variants plugin="stroke" />

<!-- ### Disabling -->
### 禁用

<Disabling plugin="stroke" />
